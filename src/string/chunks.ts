// export const splitToChunks_ = (arr, n) => {
//     return arr.length ? [arr.slice(0, n), ...splitToChunks_(arr.slice(n), n)] : [];
// };
//
// export function* splitToChunks__ (str, size) {
//     for(let i = 0; i < str.length; i+= size ) yield str.slice(i, i + size);
// }
