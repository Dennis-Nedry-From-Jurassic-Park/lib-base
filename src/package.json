{
    "name": "ms-base",
    "version": "0.0.1",
    "private": true,
    "license": "Apache-2.0",
    "scripts": {
        "yarn:clean": "yarn cache clean",
        "build:all": "yarn workspaces foreach run build",
        "api-ti-orders": "ts-node -r tsconfig-paths/register apps/ms-atr/src/api.ti.orders.ts"
    },
    "dependencies": {
        "dotenv": "^16.0.3",
        "jest": "^29.3.1",
        "path": "^0.12.7",
        "telegraf": "^4.11.2",
        "ts-jest": "^29.0.5",
        "ts-node": "^10.9.1"
    },
    "devDependencies": {
        "typescript": "^4.7.4"
    }
}
